{
  "name": "hackathon-starter-base",
  "version": "1.0.0",
  "description": "Base Hackathon Project Template configured for 4 developers (Frontend + Backend separation of concerns) with Cloud Supabase.",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
